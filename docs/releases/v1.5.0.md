# HHY v1.5.0 — Database 1.0.0 与 Runtime 集成

> 记录整理日期：2026-09-09。
> 状态：正式发行：2026-09-07；Database 独立版本为 1.0.0。
> 本地发行 tag：`v1.5.0`；对应提交：`cff7ae2c439a142abe25f3a7e3bd6be3027304bd`。

## 交付内容

- 作用域扩展资源、请求/嵌入调用成功与失败后的资源清理、取消传播和 fork-safe 扩展进程重启。
- 数据库事务闭包、Duration/BytesBuffer 转换及宿主惰性数据库 Stream。
- 配套 Database 1.0.0 提供 MySQL/PostgreSQL 有界连接池、授权远程端点、TLS、事务/保存点、预处理/批量、增量游标及精确类型。

## 兼容性与限制

旧 URL 调用保持 loopback 支持；远程连接需配置 Map 和精确授权。Windows Runtime 包不包含 Database。提交结果不确定时不能自动重放写入。RDS 实机和 24 小时长稳仍待验证；CMS 数据库夹具不等于完整 CMS 产品。

## 验收与证据口径

归档记录本地/CI 的 MySQL/PostgreSQL、AST/Bytecode、TLS 正反例、取消、租期/额度、Web scope/Worker 隔离、sanitizer 和发行校验。测试环境、持续时间与复现命令以数据库 ACCEPTANCE 为准，不外推云端认证。

## 参考记录

- [Database API](../../extensions/database/README.md)
- [Database 独立发行说明](../../extensions/database/RELEASE_NOTES.md)
- [Database 验收](../../extensions/database/ACCEPTANCE.md)
- [v1.5.0 Release](https://github.com/hh696-wq/hhy-vm/releases/tag/v1.5.0)

返回[发行记录索引](README.md)。

## English release notes

This runtime release accompanies the independently versioned database
extension 1.0. It adds scoped extension resources, fork-safe process restart,
cancellation propagation, database transaction callbacks, native Duration/binary
conversion and lazy database Streams. Web and embedded-call boundaries release
leaked database resources on success and error.

Existing database URL calls remain available. Remote data sources require explicit
configuration and endpoint authorization. Database release notes, API limits and
verification details are in extensions/database/README.md and CHANGELOG.md.

Database packages target macOS arm64 and Linux arm64/x86_64. Windows keeps the
existing runtime/sample/HTML release scope. Real RDS acceptance awaits the owner's
test configuration; local/CI success is not a claim of cloud failover certification.
